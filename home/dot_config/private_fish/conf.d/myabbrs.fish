abbr -a -- ch cht.sh
abbr -a -- j just

abbr -a -- n nvim

abbr -a -- l eza --group-directories-first --icons=auto
abbr -a -- la eza -a --git --group-directories-first --icons=auto
abbr -a -- ll eza -l --git --group-directories-first --icons=auto
abbr -a -- lla eza -la --git --group-directories-first --icons=auto

abbr -a -- rm rm -i

# Git
abbr -a -- ga git add
abbr -a -- gb git branch
abbr -a -- gc git commit
abbr -a -- gf git fetch
abbr -a -- gl git log
abbr -a -- gs git status
abbr -a -- gsw git switch
