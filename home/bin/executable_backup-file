#!/usr/bin/env bash

if [[ -z "$1" ]]; then
    echo "Usage: backup-file <file>" >&2
    exit 1
fi

if [[ ! -f "$1" ]]; then
    echo "Error: '$1' not found or not a file" >&2
    exit 1
fi

cp "$1" "$1.bak"
